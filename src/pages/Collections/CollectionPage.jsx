import { Link, useParams } from 'react-router-dom'; import Container from '../../components/common/Container'; import ProductGrid from '../../components/product/ProductGrid'; import SectionHeading from '../../components/common/SectionHeading'; import { collections } from '../../data/collections'; import { products } from '../../data/products'; import { getProductsByCollection } from '../../utils/productHelpers';
export default function CollectionPage() { const { collectionSlug } = useParams(); const collection = collections.find((item) => item.slug === collectionSlug); if (!collection) return <section className="page-section"><Container><h1>Collection not found</h1><Link to="/collections">View all collections</Link></Container></section>; return <section className="page-section"><Container><SectionHeading eyebrow="Collection" title={collection.name} description={collection.description} /><ProductGrid products={getProductsByCollection(products, collection.slug)} /></Container></section>; }
