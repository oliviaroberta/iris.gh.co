import Container from '../../components/common/Container'; import SectionHeading from '../../components/common/SectionHeading'; import { brand } from '../../data/brand';
export default function ContactPage() { return <section className="page-section"><Container><SectionHeading eyebrow="Contact" title="Let’s stay connected." description="Questions about an order, sizing, or stockists? We would like to hear from you." /><div className="contact-foundation"><p>Email: <a href={`mailto:${brand.email}`}>{brand.email}</a></p><p>Studio: {brand.location}</p></div></Container></section>; }
