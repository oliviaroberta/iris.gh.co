import Container from '../../components/common/Container'; import ImagePlaceholder from '../../components/common/ImagePlaceholder'; import SectionHeading from '../../components/common/SectionHeading';
export default function LookbookPage() { return <section className="page-section"><Container><SectionHeading eyebrow="Lookbook" title="Styling references in motion." description="A photography-led lookbook will live here once campaign imagery is available." /><div className="lookbook-grid">{[1, 2, 3, 4].map((number) => <ImagePlaceholder key={number} label={`Look ${number} coming soon`} alt={`Lookbook placeholder ${number}`} />)}</div></Container></section>; }
